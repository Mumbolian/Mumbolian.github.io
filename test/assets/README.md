Place the composite preview image for the theme picker here as `mock_theme_picker.png`.

This image should visually combine the Light and Neon themes with a diagonal divider. The modal will use this image if present; otherwise it falls back to the CSS miniature preview.
